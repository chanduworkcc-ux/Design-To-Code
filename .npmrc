auto-install-peers=false
strict-peer-dependencies=false
# tar is blocked by the Replit package firewall (security block on all versions).
# Fetch it directly from the public registry to bypass the firewall.
tar:registry=https://registry.npmjs.org/
