1. Automated & Product Notification MatrixThe system uses a . contextual behavioral engine to send automatic push notifications and emails based on live user actions:Behavioral Abandonment Triggers: If a customer opens the application, browses items, or adds products to a cart but does not check out, the system automatically fires a high-urgency notification (e.g., "Order Now before stock runs out!").Product Marketing Broadcasts: Administrators can launch specific product campaigns from their dashboard, pushing rich notifications featuring item images, descriptions, and direct deep-links to that product page.Transactional Status Updates: Automatic, system-generated updates dispatch instantly to the customer's device when order statuses change (e.g., Order Confirmed, Dispatched, or Completed).📈 2. Admin Analytics & Click-Through TrackingThe admin dashboard features a dedicated tracking ledger to monitor the exact performance and conversion rate of every sent notification:Impression Counter (Seen): Tracks the total number of unique devices where the notification was successfully delivered and displayed on the user's screen.Click-Through Counter (Clicked): Tracks the total number of users who actually tapped the notification or clicked the embedded promotional deep-link.Conversion Analytics: Automatically calculates the performance ratio (Clicks ÷ Impressions) so administrators can instantly see which marketing messages are performing best.📝 3. Mandatory App Policies (Terms & Conditions)To legally protect the platform, the application must display a standard set of legal policies during user registration, checkout screens, and within the dedicated account settings tab:Strict Return Policy: The platform operates on a absolute No-Return policy. Once an order is processed, items cannot be shipped back to the system.Strict Refund Policy: The platform enforces a strict No-Refund policy. All financial payments and coin transactions are final, irreversible, and non-reimbursable.Strict Exchange Policy: The platform enforces a strict No-Exchange policy. Users cannot trade, swap, or substitute ordered items or products for alternative variants.🏷️ 4. Product Description Enforcement BoundaryTo eliminate customer disputes and ensure absolute clarity, the system updates the global display rules for all items:Mandatory Policy Disclaimer: The exact phrase "No Refund, No Exchange, No Return" is hard-coded into the template layout of every single product description block.Immutable Visibility: This policy text is displayed in a bold,  high-contrast text block directly adjacent to the "Order Now" or checkout button, ensuring customers must see and accept the terms before making any purchase. Dynamic Remote Configuration & Forced Update EngineThe system utilizes an API-driven Remote Configuration matrix managed directly via the Admin Dashboard. This allows real-time user interface alterations and application version enforcement without requiring an app store update.A. Dynamic Asset ControlZero-Update Logo Manipulation: The admin panel hosts and changes the application's primary brand logo image web link. The client application must fetch this link during its initialization launch phase, updating the local user interface dynamically and bypassing the need for an app store submission.Asset Fallback Mechanism: The client app must maintain a local default logo inside its core files. If a network timeout or connection failure occurs during launch, the app gracefully falls back to this local asset to prevent visual bugs.B. Version Verification & Mandatory Force-Update FlowAPI Configuration Payload: On every application boot, the client fires a request to the Admin configuration endpoint. The server returns the active operational rules, including the dynamic logo link, latest app version, minimum required version, download link, and an update status indicator.The Forked Launch Gate: The client application compares its current internal version against the fetched data:Scenario A (Up-to-Date): If the local version is greater than or equal to the minimum required version, the app updates the logo image and immediately renders the main application interface dashboard.Scenario B (Outdated - Forced Update): If the local version is lower than the minimum required version, the application must immediately block user access. It intercepts the navigation routing and forces the user onto an un-skippable Forced Update Screen.Forced Update UI Requirements: The update interface must contain no bypass, close, or back navigation capabilities. It must present a prominent download button embedded with the admin-provided download link. Tapping this button routes the user directly to the external browser or target App Store to pull the mandatory build. If they do not download it, they cannot bypass this screen to view the application interface.2. Automated & Product Notification MatrixThe platform implements a contextual behavioral engine to dispatch real-time, automated push notifications and emails based on explicit user triggers and admin actions:Behavioral Abandonment Triggers: If a customer launches the application, browses available catalog products, or adds items to their shopping cart but fails to complete the checkout sequence within a designated time window, the system automatically fires a high-urgency recovery notification stating that they should order before stock runs out.Product Marketing Broadcasts: Administrators can manually orchestrate targeted product marketing campaigns from their dashboard. These entries push rich notifications featuring custom marketing copy, item images, and direct product-page deep links straight to the user base.Transactional Status Updates: Automatic, system-generated operational updates dispatch instantly to the customer's device upon database order status modifications, such as order confirmation, dispatching confirmation, or order completion.3. Admin Analytics & Click-Through TrackingThe Admin Dashboard features a centralized tracking ledger to log, monitor, and audit the real-time performance and conversion throughput of every sent notification:Impression Counter (Seen): Increments and registers the total volume of unique client devices where the push notification was successfully delivered and verified as rendered on the user's screen.Click-Through Counter (Clicked): Increments and registers the exact count of interactions where a user explicitly tapped the notification banner or accessed the embedded promotional link.Conversion Analytics Engine: The system automatically compiles and displays the click-through ratio calculated by dividing total clicks by total impressions. This metric provides administrators with instant clarity on message performance and audience conversion behavior.4. Mandatory App Policies (Terms & Conditions)To establish legal protection for the platform, the client application must display a persistent, non-negotiable set of system-wide legal policies. These are enforced globally during user registration, inside all checkout screens, and within a dedicated account settings tab:Strict Return Policy: The platform operates under an absolute No-Return Policy. Once an order enters processing status, items cannot be shipped back to the warehouse or reversed in the system.Strict Refund Policy: The platform enforces a rigorous No-Refund Policy. All financial payments, payment transactions, and in-app balance transactions are final, completely irreversible, and non-reimbursable.Strict Exchange Policy: The platform enforces a categorical No-Exchange Policy. Users are blocked from trading, swapping, or substituting ordered items or processed goods for alternative variations, colors, sizes, or product variants.5. Product Description Enforcement BoundaryTo eliminate customer disputes, minimize support overhead, and guarantee absolute consumer awareness before a financial commitment, the application structure forces global layout rules on all commercial items:Mandatory Policy Disclaimer: The exact, literal phrase "No Refund, No Exchange, No Return" is permanently fixed into the core design layout template of every single product description block. This text cannot be hidden, overwritten, or omitted by individual merchants or product creators.Immutable Visibility Block: This policy notice must be styled in a bold, high-contrast, prominent text block placed directly adjacent to the main order or final checkout buttons. This layout guarantees that consumers visually witness and implicitly agree to the finality of the terms before triggering any purchasing transaction.Act as an expert backend and frontend developer. I need you to implement and fix a strict inventory and purchase limitation system for an e-commerce application. 

Please implement or modify the existing logic to strictly adhere to the following rules:

1. ADMIN CONTROLS & MANAGEMENT:
- Provide an admin panel option to "Add Stock" (increase the quantity integer value for an item).
- Provide an admin panel option to "Mark Out of Stock" (manually change status or set quantity directly to 0).
- Provide an admin panel option to "Delete" an item. When deleted, it must perform a soft or hard delete so it is completely hidden from the public API and database queries serving customers.

2. CUSTOMER VISIBILITY RULES:
- If an item's quantity drops to 0, or is marked as out of stock by the admin, show it as "Out of Stock" to customers on the frontend, and disable the "Buy/Add to Cart" button.
- If an item is deleted by the admin, do not show or render that item to customers anywhere on the website.

3. STRICT PURCHASE LIMITS (ANTI-HOARDING RULE):
- Each customer/device can only add a quantity of EXACTLY 1 of an item to their cart. 
- A user/device can only buy exactly ONE item at a time. No unlimited buying or bulk quantities allowed.
- Prevent a single user/device from buying the same item multiple times. Track purchases via device ID/session/user ID to block users from re-purchasing an item they have already bought ("one time only").

Please provide the corrected database schema modifications, the backend validation logic (API endpoints), and the frontend component logic to enforce these constraints safely.
Admin SectionPayment Gateway Logs: Enable admins to view, track, and manage payment gateway transaction histories ("runs").Appearance Settings: Fix the broken appearance section so admins can change themes, layouts, or branding.Language & Localization: Fix the language configuration section. Ensure available languages load, appear in the UI dropdowns, and dynamically update the application text.Button & Feature Activation: Audit all admin dashboard buttons. Ensure they trigger their respective backend services, not just visual click animations.2. User Section & ProfileProfile Management: Completely rebuild the broken profile section. Users must be able to update their bio, profile picture, banner, and personal details without errors.Feature Execution: Ensure all user-facing buttons (e.g., tweet, retweet, like, follow) successfully execute their underlying code and update the database.3. Security EnhancementsAuthentication & Authorization: Secure the entire application using industry-standard protocols (e.g., JWT, session tokens).Role-Based Access Control (RBAC): Strictly isolate admin routes and API endpoints from regular users.Data Protection: Implement input validation, sanitize all user-generated content to prevent XSS, and use parameterized queries to stop SQL injection