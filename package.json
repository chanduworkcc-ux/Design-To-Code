{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); const path = require('path'); const files = ['package-lock.json', 'yarn.lock']; files.forEach(f => { try { fs.unlinkSync(path.join(process.cwd(), f)); } catch(e) {} }); const agent = process.env.npm_config_user_agent || ''; if (!agent.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/mobile run build:vercel",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "vercel-build": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/mobile run build:vercel"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "overrides": {
      "brace-expansion@1": "1.1.16",
      "brace-expansion@2": "2.1.2",
      "brace-expansion@5": "5.0.8",
      "js-yaml@3": "3.15.0",
      "js-yaml@4": "4.3.0",
      "postcss": "8.5.18",
      "fast-uri": "3.1.4",
      "fast-xml-parser": "5.10.1",
      "linkify-it": "5.0.2",
      "esbuild@0.18": "0.25.0",
      "esbuild@0.27": "0.28.1",
      "uuid@3": "11.1.1",
      "uuid@7": "11.1.1",
      "uuid@9": "11.1.1"
    }
  }
}
