{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); const path = require('path'); const files = ['package-lock.json', 'yarn.lock']; files.forEach(f => { try { fs.unlinkSync(path.join(process.cwd(), f)); } catch(e) {} }); const agent = process.env.npm_config_user_agent || ''; if (!agent.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "vercel-build": "pnpm install && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/mobile run build:vercel"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.4",
    "typescript": "~5.9.3"
  }
}
